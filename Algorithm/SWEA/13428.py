T = int(input())
# int값을 리스트로 받고 max값 구한 뒤 인덱스 바꾸기
def Max(num):
    n = int(num)
    max_num =0
    while n>0:
        if (n%10) >= max_num:
            n1 = n%10
        n = n%10
# min값 구하고 인덱스 바꿔서 최소값 만들기
# 리스트의 값을 int로 만들기
    

for i in range(T):
    N = input()